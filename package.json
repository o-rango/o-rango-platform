{
  "name": "o-rango-platform",
  "private": true,
  "version": "0.0.0",
  "description": "Agnostic Micro Front Ends Platform",
  "scripts": {
    "format": "prettier ./packages/**/**/*.ts --ignore-path ./.prettierignore --write && git status",
    "lint": "tslint -p tsconfig.json -c tslint.json \"./packages/**/*.ts\" -e \"*.spec.ts\" --fix"
  },
  "engines": {
    "node": ">= 8.9.0"
  },
  "devDependencies": {
    "lerna": "3.13.1",
    "prettier": "^1.14.2",
    "tslint": "^5.11.0",
    "typescript": "^4.0.0"
  }
}
